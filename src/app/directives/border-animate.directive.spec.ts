import { BorderAnimateDirective } from './border-animate.directive';

describe('BorderAnimateDirective', () => {
  it('should create an instance', () => {
    const directive = new BorderAnimateDirective();
    expect(directive).toBeTruthy();
  });
});
